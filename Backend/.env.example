# PostgreSQL connection (replaces the old MONGODB_URI)
DATABASE_URL=postgresql+psycopg2://tprm:tprm@localhost:5432/tprm_db

# Auth
JWT_SECRET=change_this_to_a_long_random_secret

# AI layer (Claude) — without this, the platform falls back to a heuristic score
ANTHROPIC_API_KEY=sk-ant-...
